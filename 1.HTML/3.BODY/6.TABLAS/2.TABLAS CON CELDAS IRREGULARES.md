## **Tablas HTML más complejas: `<th>` y atributos avanzados**

Como hemos visto anteriormente, crear tablas básicas en HTML es muy sencillo. Sin embargo, HTML permite construir tablas más **complejas y flexibles**, donde algunas celdas pueden abarcar varias filas o columnas, creando estructuras irregulares para organizar mejor la información.

---

### **1. La etiqueta `<th>`**

La etiqueta `<th>` se utiliza para **crear celdas de encabezado** dentro de una tabla.  
Por defecto, estas celdas se muestran **más destacadas visualmente** que las celdas normales (`<td>`), generalmente en **negrita y centradas**, para indicar al usuario que contienen títulos o categorías de datos.

Además, `<th>` puede recibir atributos que **modifican su comportamiento y aumentan la semántica y accesibilidad** de la tabla.

#### **Atributos principales de `<th>`**

|Atributo|Descripción|
|---|---|
|`scope`|Indica a qué celdas adyacentes aplica la cabecera. Mejora la accesibilidad y la relación entre encabezados y datos.|
|`abbr`|Permite establecer una **abreviatura** o información alternativa para la cabecera, útil en tablas grandes o complejas.|

---

#### **Valores del atributo `scope`**

|Valor|Descripción|
|---|---|
|`row`|La cabecera se aplica a las celdas de la fila adyacente.|
|`col`|La cabecera se aplica a las celdas de la columna adyacente.|
|`rowgroup`|La cabecera aplica a todas las celdas de un **grupo de filas**.|
|`colgroup`|La cabecera aplica a todas las celdas de un **grupo de columnas**.|
|`auto`|El navegador determina automáticamente a qué celdas aplica la cabecera.|

**Ejemplo de uso del `scope`:**

```html
<table border="1">
  <tr>
    <th>Animal</th>
    <th>Característica principal</th>
    <th>Probabilidad de dominar el mundo</th>
  </tr>
  <tr>
    <th scope="rowgroup">Gatos</th>
    <td></td>
    <td></td>
  </tr>
  <tr>
    <td>Siamés</td>
    <td>Elegante.</td>
    <td>Alta</td>
  </tr>
  <tr>
    <td>Persa</td>
    <td>Le gusta ser adorado.</td>
    <td>Alta</td>
  </tr>
  <tr>
    <th scope="rowgroup">Perros</th>
    <td></td>
    <td></td>
  </tr>
</table>
```

> Observa que aunque el `scope` está definido, visualmente **no se ve ningún cambio**; su función es semántica y mejora la accesibilidad, especialmente para lectores de pantalla.

---

### **2. Atributos que combinan o unen celdas: `colspan` y `rowspan`**

Tanto `<th>` como `<td>` pueden usar atributos para **unir celdas**, creando tablas más flexibles:

|Atributo|Descripción|
|---|---|
|`colspan`|Número de **columnas** que la celda abarcará horizontalmente.|
|`rowspan`|Número de **filas** que la celda abarcará verticalmente.|
|`headers`|Asocia la celda `<td>` con uno o varios encabezados `<th>` mediante sus IDs, mejorando la accesibilidad.|

---

### **3. Ejemplo práctico de `colspan`**

Si queremos que una cabecera abarque varias columnas, podemos usar `colspan`. Esto elimina la necesidad de añadir celdas vacías en la misma fila.

```html
<table border="1">
  <tr>
    <th>Animal</th>
    <th>Característica principal</th>
    <th>Probabilidad de dominar el mundo</th>
  </tr>
  <tr>
    <th scope="rowgroup" colspan="3">Gatos</th>
  </tr>
  <tr>
    <td>Siamés</td>
    <td>Elegante.</td>
    <td>Alta</td>
  </tr>
  <tr>
    <td>Persa</td>
    <td>Le gusta ser adorado.</td>
    <td>Alta</td>
  </tr>
  <tr>
    <th scope="rowgroup" colspan="3">Perros</th>
  </tr>
</table>
```

> Aquí, la celda “Gatos” y la celda “Perros” **abarcan las tres columnas**, logrando una separación clara de categorías sin celdas vacías adicionales.

---
