# **Listas de Selección en HTML**

En formularios HTML, además de las **casillas de verificación (`<input type="checkbox">`)** y los **botones de opción (`<input type="radio">`)**, existe otra forma de presentar varias opciones: **las listas de selección**.  
Estas listas son útiles cuando:

- Hay **muchas opciones disponibles** que no pueden mostrarse cómodamente con checkboxes o radios.
    
- Se busca un **diseño más compacto y visualmente ordenado**.
    

Existen **dos tipos principales de listas seleccionables**:

|**Tipo de información**|**Etiqueta HTML**|**Ejemplo**|
|---|---|---|
|Lista desplegable|`<select>`|Combo tradicional|
|Lista abierta|`<datalist>`|Input con autocompletado|

En esta explicación nos centraremos en **`<select>`**, ya que `<datalist>` se abordará más adelante.

---

## **1. Listas desplegables con `<select>`**

La forma más básica de crear una lista desplegable es utilizando una etiqueta `<select>` que contenga en su interior varias etiquetas `<option>`.

### **Sintaxis básica**

```html
<form method="post" action="/send/">
  Selecciona la opción deseada:
  <select>
    <option value="1">Opción 1</option>
    <option value="2">Opción 2</option>
    <option value="3">Opción 3</option>
  </select>
</form>
```

**Explicación:**

- `<select>`: contenedor de todas las opciones.
    
- `<option>`: representa cada opción que podrá seleccionar el usuario.
    
- `value`: valor interno que se enviará al servidor cuando se seleccione la opción.
    
- Contenido de `<option>`: texto que el usuario verá en el formulario.
    

---

## **2. Opción seleccionada por defecto**

Para que una opción aparezca **seleccionada al cargar el formulario**, se utiliza el atributo **`selected`**.

### **Ejemplo**

```html
<form method="post" action="/send/">
  Selecciona la opción deseada:
  <select>
    <option value="1">Opción 1</option>
    <option value="2" selected>Opción 2</option>
    <option value="3">Opción 3</option>
  </select>
</form>
```

En este caso, **"Opción 2"** estará seleccionada automáticamente.

**Nota:**  
No confundas `selected` con `checked`, ya que este último solo se aplica a `checkbox` y `radio`.

---

## **3. Listas seleccionables múltiples**

Si queremos que el usuario **pueda elegir más de una opción**, añadimos el atributo **`multiple`** a `<select>`.

### **Ejemplo**

```html
<form method="post" action="/send/">
  Selecciona una o varias opciones:
  <select multiple>
    <option value="1">Opción 1</option>
    <option value="2">Opción 2</option>
    <option value="3">Opción 3</option>
    <option value="4">Opción 4</option>
  </select>
</form>
```

**Cómo funciona:**

- Permite seleccionar múltiples opciones al mismo tiempo.
    
- En la mayoría de navegadores:
    
    - **Ctrl (Windows/Linux)** o **Cmd (macOS)** → Selección múltiple individual.
        
    - **Shift** → Selección de un rango completo.
        

---

## **4. Personalización básica del `<select>`**

La etiqueta `<select>` tiene un estilo predeterminado definido por el navegador. Para personalizarlo, podemos usar la propiedad **`appearance`** y otros estilos CSS.

### **Ejemplo de personalización**

```css
select.custom {
  appearance: none; /* Elimina estilos predeterminados */
  width: fit-content;
  padding: 0.5rem 2.5rem 0.5rem 1rem;
  border-radius: 4px;
  font-family: Jost, sans-serif;
  font-size: 1rem;
  color: indigo;
  border: 2px solid indigo;
  background: url(data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSIzMiIgaGVpZ2h0PSIzMiIgdmlld0JveD0iMCAwIDI0IDI0Ij48cGF0aCBmaWxsPSIjMDAwIiBkPSJtMTIgMTVsLTUtNWgxMHoiLz48L3N2Zz4=) 
    center right 5px no-repeat;
  background-size: 20px;
}
```

**Explicación de estilos clave:**

- `appearance: none;` → elimina el estilo predeterminado del navegador.
    
- `background` → agrega un ícono personalizado como flecha.
    
- `background-size` → ajusta el tamaño del ícono.
    
- `border`, `color`, `font-family` → definen colores y tipografía.
    

---

### **Código SVG de la flecha**

El ícono utilizado está codificado en **Base64** dentro del CSS. Su código original es el siguiente:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24">
  <path fill="#000" d="m12 15l-5-5h10z"/>
</svg>
```

Este método permite incrustar la imagen sin depender de un archivo externo, optimizando el rendimiento.

---

## **5. Limitaciones en la personalización**

Aunque podemos modificar varios aspectos, la personalización de `<select>` es **limitada**:

- Se pueden cambiar tipografías, colores de texto y fondo.
    
- Es más complejo alterar el diseño interno de `<option>` debido a restricciones de los navegadores.
    

Para personalizaciones avanzadas, suele optarse por **plugins o librerías** que repliquen el comportamiento de un `<select>` con más libertad de estilos.

---