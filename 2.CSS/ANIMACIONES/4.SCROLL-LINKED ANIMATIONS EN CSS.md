
##  1. ¿Qué son las animaciones vinculadas al scroll o visibilidad?

Son animaciones que **no dependen del tiempo**, sino que se **sincronizan con el scroll del usuario** o con la **visibilidad de un elemento en el viewport**.

> En lugar de usar `animation-duration`, se controlan con el desplazamiento o la entrada del elemento a la pantalla.

---

##  2. Tipos de timeline

|Tipo|Explicación|
|---|---|
|`scroll-timeline`|La animación avanza según el **scroll de un contenedor**|
|`view-timeline`|La animación avanza según la **entrada/salida del elemento** en la pantalla|

---

##  3. Propiedades comunes

### `animation-timeline`

- Indica qué línea de tiempo debe seguir la animación.
    
- Puede ser:
    
    - una **línea personalizada** (`miLinea`)
        
    - una función automática como `scroll()` o `view()`
        

```css
.elemento {
  animation-name: mover;
  animation-timeline: scroll();
}
```

---

## 4. Scroll Timeline: animaciones controladas por el scroll

### A. `scroll-timeline-name`

- Asigna un **nombre** a la línea de tiempo generada por el scroll del contenedor.
    

```css
.scroll-container {
  scroll-timeline-name: miScroll;
}
```

### B. `scroll-timeline-axis`

- Indica si el scroll es:
    
    - `block` → vertical
        
    - `inline` → horizontal
        

```css
.scroll-container {
  scroll-timeline-axis: block;
}
```

### C. Uso de la línea de tiempo

```css
.elemento {
  animation-name: mover;
  animation-timeline: miScroll;
}
```

---

##  5. View Timeline: animaciones según visibilidad

### A. `view-timeline-name`

- Asigna un nombre a la línea de tiempo generada **por la visibilidad del elemento**.
    

```css
.caja {
  view-timeline-name: verCaja;
}
```

### B. `view-timeline-axis`

- Define el eje que se usará para medir la visibilidad:
    
    - `block` → vertical
        
    - `inline` → horizontal
        

```css
.caja {
  view-timeline-axis: block;
}
```

### C. `view-timeline-inset`

- Define un área interna del viewport donde se empieza a considerar la visibilidad.
    

```css
.caja {
  view-timeline-inset: 10%;
}
```

> Cuanto más grande el `inset`, más tardará la animación en comenzar.

---

##  6. Alternativa: `@scroll-timeline`

En lugar de usar `scroll-timeline-name`, se puede definir una línea de tiempo personalizada:

```css
@scroll-timeline miScroll {
  source: auto;
  orientation: block;
  scroll-offsets: start 0%, end 100%;
}
```

|Propiedad|Función|
|---|---|
|`source`|El contenedor que genera el scroll|
|`orientation`|Dirección del scroll (`block` o `inline`)|
|`scroll-offsets`|Define el rango del scroll que se usará para animar|

Uso:

```css
.elemento {
  animation-name: mover;
  animation-timeline: miScroll;
}
```

---

##  7. Funciones automáticas

### A. `scroll()`

- Crea una línea de tiempo basada en el scroll general del documento o contenedor.
    

```css
.elemento {
  animation-timeline: scroll();
}
```

### B. `view()`

- Crea una línea de tiempo basada en **cuándo el elemento entra/sale del viewport**.
    

```css
.elemento {
  animation-timeline: view();
}
```

---

##  8. Diferencias clave

|Propiedad de scroll clásica|Scroll/View Timeline|
|---|---|
|`overflow`, `scroll-behavior`, `scroll-snap-*`|Controlan el comportamiento visual del scroll|
||`scroll-timeline-name`, `view-timeline-name`, `inset`, etc.|
||Controlan **cómo el scroll o la visibilidad afectan la animación**|

---

##  9. Ejemplo completo: scroll-timeline

```css
@keyframes mover {
  from { transform: translateX(0); }
  to { transform: translateX(300px); }
}

.scroll-container {
  overflow-y: scroll;
  height: 400px;
  scroll-timeline-name: miScroll;
  scroll-timeline-axis: block;
}

.caja {
  animation-name: mover;
  animation-timeline: miScroll;
  animation-duration: auto;
}
```

---

##  10. Ejemplo completo: view-timeline

```css
@keyframes aparecer {
  from { opacity: 0; transform: scale(0.8); }
  to { opacity: 1; transform: scale(1); }
}

.elemento {
  view-timeline-name: visibilidad;
  view-timeline-axis: block;
  view-timeline-inset: 10%;

  animation-name: aparecer;
  animation-timeline: visibilidad;
  animation-duration: auto;
}
```

---
