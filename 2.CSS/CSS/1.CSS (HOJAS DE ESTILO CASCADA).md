Usamos para agarrar cada componente de html y darle un estilo y que el conjunto de estilo que damos a cada elemento nos dé una pagina. nos importa saber las relaciones de los elementos.

tenemos tres formas de modificar códigos html de las cuales dos no son tan recomendables.

El estilo en linea: su nombre indica que es una propiedad dentro de una linea

`<h1 style="color: red;font-size: 20px;" > titulo <h1>`

acá podemos ver que aplicamos un lenguaje css dentro de html el cual queda mezclado lenguajes de html y css por lo cual no es recomendable.

la otra forma es usar el atributo style no como atributo sino como etiqueta
entonces abrimos la etiqueta style y abrimos una llave y seleccionamos que el titulo tenga color azul ej:

`<h1> titulo </ h1>`
`<style>`
     `h1 {`
         `color: azul`
        `}`
`</style>`

Aun asi esta forma no es recomendable porque las propiedades que solemos usar son demasiadas, entonces  la forma recomendable seria crear una carpeta y enlazarla a la hoja principal usando una etiqueta llamada "link" que linquea una hoja de estilos
que se usa en el head.
ej:

`<!doctype html>`
`<html>`
    `<head>`
          `<title>enlazados css</title>`
    `<link rel="stylesheet" href="style.css">`
    `</head>`
`</html>.`

asi tendríamos linkeado el apartado de style con la hoja principal.

**re**l : viene de ralationship y le esta diciendo al sistema que clase de archivo voy a vincular, que relacion tiene el archivo a vincular. entonces le ponemos que vamos a vincular una hoja de estilos "stylesheet".
 usamos la otra propiedad href que indicamos la ruta de donde proviene el archivo.
 
# SELECTORES (básicos).

Usamos los los selectores para marcar un conjunto o una linea para poder modificarlo.

Podemos seleccionar por **CLASES** que nos referimos a un grupo especifico que queremos diferenciar del resto asi facilita modificar solamente dicha clase y no toda la linea entera de escritura . ej: si yo tengo una lista de ingredientes y quiero que los ingredientes faltantes sean de un determinado color y los que si están sean de otros, creo la propiedad CLASS ejemplo

`<h2> ingredientes </h2z>`
`<ul>`
     `<li class="faltante">harina</li>`
     `<li>huevo</li>`
     `<li>sal</li>`
     `<li>azucar</li>`
     `<li>leche</li>`
`</ul>`

Acabe poniéndole que la harina esta en la clase faltante entonces lo separe de los demás y facilitando modificarlo en ccs de la siguiente forma

.faltante {
      color: red 
}

cabe aclara usar el punto primero y luego escribir el nombre de la clase porque de lo contrario tomaría como etiqueta y no cómo clase.
también es muy usado la etiqueta spam que se utiliza sobre una linea para seleccionarlo mas rápido ejemplo

`<h2> lista de <spam> compras </spam> </h2>`

En css quedaria asi

`<spam> {`
      `color: red`
`}`

De esta forma solo cambiamos el elemento que esta dentro de **spam.**

Tenemos **selectores por ID** que en este caso solo podemos seleccionar un objeto con una sola característica no podemos nombrar dos cosas con el atributo id que tengan la misma clase. Cada cosa que usa como selector id debe ser diferente en su clase.

`<h2>ingredientes</h2>`
     `<ul>`
         `<li id="ingrediente a comprar"> leche</li>`
         `<li id="ingrediente sobrante">huevo</li>`
    `</ul>`

Entonces aplico distintos selectivos con el atributo id y asi los diferencio el uno del otro usando esta atrubuto y en css aplicariamos asi

para este atributo no usamos un punto usamos el hashtag y ponemos el nombre que le dimos a la id

`#ingrediente a comprar {`
     `color: verde azulado`
`}`

`#ingrediente sobrante {`
     `color: amarillo`
`}`

de esta forma modifique el atributo id pero no los dos a la vez cada uno tiene una modificación diferente porque por mas que tengan id no tienen la misma clase o función.

La otra forma de seleccionar es la mas sencilla debido a que **seleccionamos por etiquetas** es decir que al momento de usar css seleccionamos la etiqueta a modificar 
ej

h2 {
  color:  red 
}

de esta forma va a modificar todo las etiquetas con h2.




# SELECTORES (AVANZADOS)

Como vimos anteriormente que son los selectores; podemos definir otras formas mas complejas de seleccionar elementos:

### SELECTORES POR ATRIBUTOS

img [src="descarga.png"]{
	width: 100%;
}

De esta forma modificamos a todas las imágenes que tengan el atributo src="descarga.png", tambien tenemos esta forma:

img [src$="png"]{
	width: 100%;
}

Asi logramos darle estilo a  los src que terminen con un valor con png (u otro formato establecido) se modifiquen. otra forma seria:

img [src^="descarga"]{
	width: 100%;
}

De esta manera modificamos las imágenes que de atributo y valor definido tengan descarga.



### SELECTORES COMBINADORES (JERARQUICOS):


#### **SELECTORES DESCENDIENTES**

Los selectores descendientes hace que la etiqueta que este dentro de otra pueda ser modificada: ejemplo:

ul strong{
	color: red;
}

Dentro de la etiqueta `<ul>` se encuentra `<li>` y dentro de ella se encuentra `<strong>`
entonces de esta manera le decimos al navegador que modifique la etiqueta **strong** que se encuentra en alguna parte dentro de la etiqueta  **ul** .

#### **SELECTOR HIJO DIRECTO**

De esta forma solo modificamos a las etiquetas que son hijos directos como el caso de las listas.
`<ul>` tiene como hijo directo a `<li>` por lo que en css haríamos

ul > li {
	color: red;
}

#### **SELECTORES HERMANOS ADYACENTES** 

Esta forma de seleccionar hace que la etiqueta que este delante de una pueda ser modificada, pero solo a esa primer etiqueta.

h2 + p {
	color: red;
}

si luego del párrafo hay una etiqueta de párrafo no será editada a menos que esta este al frente de un h2, de lo contrario no será editado .

#### **SELECTORES DE HERMANOS GENERALES**

Con esta forma logramos que las etiqueta que vengan después de una puedan ser modificadas sin importar que sigan uno después del otro , es decir que si tenemos un `<h2`> y le sigue un párrafo y siguiente le sigue un div y siguiente un párrafo otra vez, ambos párrafos van a ser editados de igual forma porque estamos declarando que todo los párrafos que vengan después  del `<h2>` puedan sean modificados.

h2 ~ p {
	color: red;
}


## **SELECTORES DE MULTIPLES ETIQUETAS**

Cuando queremos agregar las mismas características  a varias etiquetas, solo debemos de ponerlas juntas en una misma linea y separarlos con una coma:

h1, h2, p {
	COLOR: RED; 
} .

Asi concluiríamos con los tipos de selectores.


# ¿Qué es una pseudoclase en CSS?

Una **pseudoclase** en CSS se usa para definir un **estado especial de un elemento**, **sin necesidad de agregar una clase en HTML**.

###  Ejemplo:

`a:hover {   color: red; }`

🟢 Esto aplica color rojo **solo cuando el mouse está encima del enlace**. Ese estado se llama una **pseudoclase**.

---

##  ¿Cómo se usa?


`selector:pseudoclase {   propiedad: valor; }`

Puedes combinarlas:

`p:first-child:hover {   color: blue; }`

---

## 🧾 Lista de pseudoclases más importantes

### 🔹 Pseudoclases de interacción (usuario):

|Pseudoclase|¿Qué hace?|
|---|---|
|`:hover`|Cuando el cursor pasa sobre el elemento|
|`:active`|Cuando el elemento está siendo clicado|
|`:focus`|Cuando el elemento recibe foco (ej. un input seleccionado)|
|`:focus-visible`|Similar a `:focus`, pero solo si es visible para teclado|
|`:focus-within`|Se aplica si el elemento o un hijo tiene el foco|
|`:visited`|Enlaces que ya han sido visitados|
|`:link`|Enlaces no visitados|

---

### 🔹 Pseudoclases estructurales (basadas en posición en el DOM):

|Pseudoclase|¿Qué hace?|
|---|---|
|`:first-child`|Si es el primer hijo de su padre|
|`:last-child`|Si es el último hijo de su padre|
|`:nth-child(n)`|Aplica al hijo número `n`|
|`:nth-last-child(n)`|Igual que arriba pero desde el final|
|`:only-child`|Si es el único hijo de su padre|
|`:first-of-type`|Primer hijo de su tipo (por ejemplo, el primer `<p>`)|
|`:last-of-type`|Último hijo de su tipo|
|`:nth-of-type(n)`|Aplica al `n`-ésimo hijo de un tipo específico|
|`:only-of-type`|Si es el único hijo de su tipo|

---

### 🔹 Pseudoclases de formulario:

|Pseudoclase|¿Qué hace?|
|---|---|
|`:enabled`|Campos habilitados|
|`:disabled`|Campos deshabilitados|
|`:checked`|Radios o checkboxes marcados|
|`:indeterminate`|Estado mixto (como casilla parcialmente seleccionada)|
|`:valid`|Campo de formulario válido|
|`:invalid`|Campo de formulario inválido|
|`:required`|Campo requerido|
|`:optional`|Campo no requerido|
|`:read-only`|Campo de solo lectura|
|`:read-write`|Campo editable|

---

### 🔹 Pseudoclases de contenido o estado general:

|Pseudoclase|¿Qué hace?|
|---|---|
|`:empty`|Elemento sin hijos ni contenido|
|`:not(selector)`|Excluye un selector|
|`:is(selector)`|Simplifica múltiples selectores (mejor para rendimiento)|
|`:where(selector)`|Igual que `:is()` pero sin peso de especificidad|
|`:has(selector)`|Selecciona si contiene ciertos hijos (muy poderosa, reciente)|

Actualmente existen 3 nuevas pseudoclases que facilitan la tarea o la simplicidad de agrupar elementos.
Las pseudoclases `:is()`, `:has()` y `:where()` son **más modernas** y **muy poderosas**, especialmente cuando trabajas con estructuras complejas en HTML. Vamos a verlas bien explicadas:

---

## 🔷 `:is()` — **Selector lógico de coincidencia**

### 📌 ¿Qué hace?

Simplifica varios selectores que comparten un mismo bloque de estilos.

### 🧠 Sintaxis:

`:is(h1, h2, h3) {   color: blue; }`

Esto aplica `color: blue` a **todos esos elementos** sin tener que escribir:

`h1, h2, h3 {   color: blue; }`

También puede combinarse con otros selectores:

`:is(article, section) p {   font-style: italic; }`

###  Ventajas:

- Reduce código repetitivo.
    
- Mejora la legibilidad.
    

## `:where()` — **Como `:is()` pero sin especificidad**

### ¿Qué hace?

Funciona igual que `:is()`, **pero siempre tiene especificidad 0**.  
Esto significa que **nunca ganará un conflicto por especificidad**, útil para resets o estilos genéricos.

### 🧠 Ejemplo:

`:where(h1, h2, h3) {   margin: 0; }`

Usas esto cuando quieres aplicar estilos básicos, pero permitir que otros más específicos los sobrescriban fácilmente.

---

## `:has()` — **Pseudoclase de “padre que contiene”**

> ⚠️ Requiere navegadores modernos (ya soportado por la mayoría: Chrome, Edge, Safari, Firefox desde 2023).

###  ¿Qué hace?

Permite seleccionar un **elemento que contiene otro**.

###  Ejemplo:

`div:has(img) {
border: 2px solid green; 
}`

 Esto selecciona cualquier `<div>` que **tenga al menos una imagen dentro**.

Más complejo:

`article:has(h2 + p) { 
background-color: #eef; 
}`

Esto aplicará estilos solo si un `<h2>` va seguido por un `<p>` dentro de un `<article>`.

## Resumen:

|Pseudoclase|¿Qué hace?|Especificidad|
|---|---|---|
|`:is()`|Agrupa selectores y aplica estilos|La del selector más específico|
|`:where()`|Igual que `:is()`, pero sin peso de especificidad|Siempre `0`|
|`:has()`|Selecciona padres que contienen ciertos hijos|Normal (como cualquier otro selector)|

---
# ¿Qué es un **pseudo-elemento** en CSS?

Un **pseudo-elemento** permite **estilizar partes específicas de un elemento** que no están explícitamente presentes en el HTML, como la **primera letra**, la **primera línea**, o **insertar contenido antes o después** del texto de un elemento.

> Son útiles para aplicar estilos especiales **sin modificar el HTML**.

---

## 🧠 Diferencia entre pseudoclase y pseudo-elemento

|Característica|Pseudoclase (`:hover`, `:focus`)|Pseudo-elemento (`::before`, `::first-line`)|
|---|---|---|
|Aplica a...|Estados o condiciones del elemento|Partes de un elemento|
|Sintaxis|Una sola `:`|Doble `::` (aunque `:` todavía funciona por compatibilidad)|
|Ejemplo|`a:hover`|`p::first-line`|

---

## 🔷 ¿Cómo se escribe un pseudo-elemento?

### 📌 Sintaxis:

`selector::pseudo-elemento {   propiedad: valor; }`

---

## 🧾 Lista detallada de pseudo-elementos principales

---

### 🔹 `::before` y `::after`

🔸 **¿Qué hacen?**  
Insertan contenido virtual **antes o después** del contenido real de un elemento.

🔸 **Requiere** la propiedad `content`.

#### Ejemplo:

`h1::before {   
content: "🔥 ";
color: red; } 

h1::after {
content: " ✅";
color: green; }`

🔹 Esto hace que si tienes:

`<h1>Aprendiendo CSS</h1>`

Verás: 🔥 Aprendiendo CSS 

---

### 🔹 `::first-line`

🔸 **¿Qué hace?**  
Aplica estilos **solo a la primera línea** visible de un párrafo (según el ancho del contenedor).

#### Ejemplo:

`p::first-line {
	font-weight: bold;
	color: navy;
}`

 Solo la primera línea del `<p>` tendrá ese estilo, aunque el contenido continúe.

> **Limitaciones:** Solo puedes usar ciertas propiedades como `font`, `color`, `text-decoration`, etc.

---

### 🔹 `::first-letter`

🔸 **¿Qué hace?**  
Aplica estilo **solo a la primera letra** de un elemento de texto.

#### Ejemplo:

`p::first-letter {
	font-size: 2em; 
	color: crimson; 
}`

 Muy usado en diseños editoriales (como libros o revistas).

---

### 🔹 `::selection`

🔸 **¿Qué hace?**  
Estiliza el **texto seleccionado por el usuario** (cuando haces clic y arrastras para seleccionar texto).

#### Ejemplo:

`::selection {   background: yellow;   color: black; }`

🔸 Esto cambia el color de fondo del texto cuando el usuario lo selecciona.

---

### 🔹 `::placeholder`

🔸 **¿Qué hace?**  
Estiliza el texto de **placeholder** en un `<input>` o `<textarea>`.

#### Ejemplo:

`input::placeholder {   color: gray;   font-style: italic; }`

---

### 🔹 Otros pseudo-elementos (menos comunes o específicos)

| Pseudo-elemento                       | Uso                                                                 |
| ------------------------------------- | ------------------------------------------------------------------- |
| `::marker`                            | Estiliza los íconos o bullets de listas (`<li>`)                    |
| `::backdrop`                          | Estiliza el fondo de elementos como modales (`<dialog>`)            |
| `::cue`                               | Estiliza subtítulos en `<video>` con pistas                         |
| `::file-selector-button`              | Estiliza el botón de selección de archivo en `<input type="file">`  |
| `::grammar-error`, `::spelling-error` | Para corregidores automáticos en navegadores compatibles            |
