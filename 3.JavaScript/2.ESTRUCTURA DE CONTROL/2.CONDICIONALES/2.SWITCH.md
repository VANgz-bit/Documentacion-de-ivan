## ¿Qué es `switch`?

`switch` es una **estructura de control condicional**, similar a `if`, que permite tomar decisiones en base al **valor de una expresión o variable**.  
Se utiliza cuando una misma expresión puede tener **varios valores posibles**, y cada uno de ellos requiere un bloque de código diferente.

En lugar de escribir una cadena larga de `if...else if...else`, `switch` ofrece una forma **más clara, ordenada y legible** de manejar múltiples opciones.

---

## Sintaxis general de `switch`

```js
switch (expresión) {
  case valor1:
    // Bloque de código si expresión === valor1
    break;

  case valor2:
    // Bloque de código si expresión === valor2
    break;

  // Se pueden agregar más casos según sea necesario

  default:
    // Bloque de código si ningún case coincide
}
```

---

## Desglose de cada parte

### 1. `switch (expresión)`

- Es la expresión o variable que se va a evaluar.
    
- JavaScript compara este valor con cada `case` usando **comparación estricta** (`===`).
    
- Ejemplo: si escribimos `switch (color)`, el valor de la variable `color` será comparado contra cada `case`.
    

---

### 2. `case valor:`

- Representa una **posible coincidencia** con la expresión del `switch`.
    
- Si la expresión es igual al valor del `case`, se ejecuta el bloque de código correspondiente.
    
- Puede haber tantos `case` como sean necesarios.
    

```js
case "rojo":
  console.log("El color es rojo");
  break;
```

---

### 3. `break`

- Es una palabra clave que **detiene la ejecución** del `switch`.
    
- Si no se coloca, el programa seguirá ejecutando los siguientes `case`, aunque no coincidan.
    
- Este comportamiento se llama **fall through** (caída hacia abajo).
    
- Por eso, normalmente se incluye `break` al final de cada caso.
    

Ejemplo:

```js
case "verde":
  console.log("El color es verde");
  break; // Detiene la ejecución aquí
```

---

### 4. `default`

- Es un bloque **opcional** que se ejecuta si **ningún `case` coincide** con la expresión.
    
- Es equivalente al `else` en las estructuras condicionales.
    

```js
default:
  console.log("Color no reconocido");
```

---

## Ejemplo práctico

```js
let dia = "martes";

switch (dia) {
  case "lunes":
    console.log("Es el primer día de la semana");
    break;

  case "martes":
    console.log("Es el segundo día de la semana");
    break;

  case "miércoles":
    console.log("Es mitad de semana");
    break;

  default:
    console.log("No es un día laboral registrado");
}
```

**Explicación del ejemplo**:

1. El valor de `dia` es `"martes"`.
    
2. El `switch` busca un `case` que coincida exactamente con `"martes"`.
    
3. Cuando lo encuentra, ejecuta `console.log("Es el segundo día de la semana")`.
    
4. Luego, gracias al `break`, la ejecución del `switch` termina.
    

---

## ¿Qué pasa si NO usamos `break`?

```js
let fruta = "naranja";

switch (fruta) {
  case "manzana":
    console.log("Elegiste una manzana");

  case "naranja":
    console.log("Elegiste una naranja");

  case "banana":
    console.log("Elegiste una banana");

  default:
    console.log("Fruta no encontrada");
}
```

**Resultado**:

```
Elegiste una naranja
Elegiste una banana
Fruta no encontrada
```

**¿Por qué ocurre esto?**  
Porque al faltar `break`, el código no se detiene y sigue ejecutando los demás `case`.  
Esto es el **fall through**.  
En la mayoría de los casos no se desea este comportamiento, por lo que se recomienda usar `break`.

---

## Comparación rápida: `switch` vs `if`

|Característica|`switch`|`if`|
|---|---|---|
|Tipo de comparación|Solo igualdad estricta (`===`)|Comparaciones de todo tipo (`>`, `<`, `!=`, `&&`, etc.)|
|Flexibilidad|Limitado a comparar un único valor|Permite condiciones complejas|
|Claridad|Más limpio con múltiples opciones de un mismo valor|Se vuelve extenso con muchos `else if`|
|Caso típico de uso|Seleccionar entre varias opciones basadas en una variable|Evaluar expresiones y condiciones más variadas|

---

## Cuándo usar `switch`

Se recomienda utilizar `switch` cuando:

- Se evalúa una **sola variable o expresión** contra distintos valores posibles.
    
- Existen **muchas alternativas** que hacen el código con `if...else if` demasiado largo.
    
- Se busca una **estructura clara y organizada** para representar esas opciones.
    

---

## Ejemplo completo con `if/else` y `switch`

```js
let opcion = 2;

// Usando if/else
if (opcion === 1) {
  console.log("Opción uno seleccionada");
} else if (opcion === 2) {
  console.log("Opción dos seleccionada");
} else if (opcion === 3) {
  console.log("Opción tres seleccionada");
} else {
  console.log("Opción no válida");
}

// Usando switch
switch (opcion) {
  case 1:
    console.log("Opción uno seleccionada");
    break;
  case 2:
    console.log("Opción dos seleccionada");
    break;
  case 3:
    console.log("Opción tres seleccionada");
    break;
  default:
    console.log("Opción no válida");
}
```

En este ejemplo, con varias opciones posibles, `switch` resulta **más limpio y fácil de leer** que una cadena de `if...else if`.

---
